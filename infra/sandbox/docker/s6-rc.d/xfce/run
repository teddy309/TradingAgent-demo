#!/command/with-contenv sh
set -eu

export HOME=/opt/data
export USER=hermes
export LOGNAME=hermes
export DISPLAY=:1
export XDG_RUNTIME_DIR=/opt/data/run

until xdpyinfo -display "${DISPLAY}" >/dev/null 2>&1; do
    sleep 1
done

exec s6-setuidgid hermes dbus-run-session -- startxfce4
